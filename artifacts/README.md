# Artifacts

This directory contains generated intermediate artifacts retained from the original competition workflow.

`feature_matrices/results_combined.csv` is a large generated feature table from the handcrafted-feature experiments. It is kept separate from `results/`, which contains compact evaluation summaries.
